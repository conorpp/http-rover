
each person in queue
    - if (person.position == 1 && person.start){
    -   var freshTime = person.time - Math.floor((new Date().getTime() - person.start)/1000);
    - }else var freshTime = person.time;
    tr(id='pos'+person.position, class='queueMember')
        td.pos #{person.position}
        td #{person.name}
        td.timer #{freshTime}
    